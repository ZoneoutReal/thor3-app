# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

This is the native Rukr app (Expo SDK 56 / React Native), mirroring the Rallo
pipeline at `~/TotemDigital/lacrosse-app/apps/mobile/`. It shares the existing
Supabase backend (project `thor3`) with the Next.js PWA at the repo root, via the
family-passcode-gated edge functions. Never push to main; ship through PRs.
